{{ header }}
<div id="common-success" class="container">
  <div class="row">
    <div id="content" class="{{ class }}">{{ content_top }}
      <h1>{{ heading_title }}</h1>
      {{ text_message }}
      <div class="buttons">
        <div class="pull-right"><a href="{{ continue }}" class="btn btn-continue">{{ button_continue }}</a></div>
      </div>
      </div>
    </div>
</div>
{{ footer }}